// Scroll behavior to smooth transition
document.querySelector('.email-form button').addEventListener('click', function () {
    document.getElementById('trendingSection').scrollIntoView({ behavior: 'smooth' });
});
